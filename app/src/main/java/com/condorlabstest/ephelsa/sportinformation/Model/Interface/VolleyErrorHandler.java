package com.condorlabstest.ephelsa.sportinformation.Model.Interface;

public interface VolleyErrorHandler {

    // Only used to close the windows and show the error.

    void errorHandler(String error);
}
